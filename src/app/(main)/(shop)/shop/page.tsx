import Shop from "../_components/Shop";

export default async function Home() {
    // const recipes = await getRecipes();
    return (
        <div className="">
            <Shop />
        </div>
    );
}
